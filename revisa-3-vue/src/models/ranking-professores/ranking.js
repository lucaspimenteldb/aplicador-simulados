export default class Ranking {
    tipo;

    colocacao;

    pontos;

    constructor (tipo, colocacao, pontos) {
      this.tipo = tipo;
      this.colocacao = colocacao;
      this.pontos = pontos;
    }
}
